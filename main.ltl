// ltl-svm-linear/main.ltl
//
// Linear SVM with hinge loss + SGD trainer.
//
// This is an early skeleton — contribution is welcome! See the
// README for build instructions.

import std::io
import std::fmt

pub fn main(args: [str]) -> Int {
    io::println("ltl-svm-linear v0.1.0")
    io::println("Linear SVM with hinge loss + SGD trainer.")
    0
}
