// examples/demo.ltl — runnable demo for ltl_svm_linear

import std::io
use ltl_svm_linear as lib

fn main() {
    io::println("ltl_svm_linear demo starting...")
    let result = lib::main() |> io::inspect()
    io::println("demo complete")
}